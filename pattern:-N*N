#include <iostream>
using namespace std;
int main() {
    int row,col,a,b;
    cout<<"enter the value of row and column"<<endl;
    cin>>row>>col;
    for(int a=1;a<=row;a++){
        for(int b=1;b<=col;b++){
            cout<<"*";
        }
        cout<<"\n";
    }
  return 0;
}
